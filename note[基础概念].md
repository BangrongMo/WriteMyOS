[TOCM]

[TOC]

# FAT32:
### 1、mbr bootloader加载分区表，从每个分区描述的开头查找活动分区
### 2、文件系统中用4个字节描述一个簇，簇往往在4096b~128k之间
### 3、从分区表找到活动的分区
mbr分区表lbe地址（一行16字节）:
第一个000001be  80 00 15 00 0c 03 e0 ff  14 00 00 00 ec e7 0e 00                                                起始LBA在 offset 8–11，即这部分：14 00 00 00
第二个000001ce  
第三个000001de  
第四个000001ee  ，哪个地址后面跟上 0x80即表示为活动分区
### 4、进入活动分区，读取第一个扇区前90个字节，用于计算数据区位置
FAT32 参数解析

从起始 LBA 处读取第一个扇区，这是 FAT32 的 Boot Sector，里面有很多关键参数：

| 参数名称            | 偏移位置  | 描述                         |
|---------------------|-----------|------------------------------|
| BytesPerSector      | 0x0B      | 每扇区字节数（通常 512）     |
| SectorsPerCluster   | 0x0D      | 每 Cluster 包含的扇区数      |
| ReservedSectorCount | 0x0E      | Boot + FAT 前的保留扇区数    |
| NumFATs             | 0x10      | FAT 表数量（通常是 2）       |
| FATSize             | 0x24      | 每个 FAT 表所占扇区数        |
| RootCluster         | 0x2C      | 根目录起始 cluster 编号      |

### 注意：如果位置偏移不连续，代表要读多个字节 比如0x0B下一行是0x0D，代表 BytesPerSector要读0x0B  0x0C
### 由于是小端设计，先读到的是低位
### 计算数据区起始 LBA
```
DataRegionLBA = Partition_Start_LBA
              + ReservedSectorCount
              + (NumFATs × FATSize)

```

文件系统中用4个字节描述一个簇，簇往往在4096b~128k之间 ，本系统中统一使用4k一簇

# FAT数据区:
### 数据区开始就是文件系统根目录，每个文件，8字节作为文件名，3个字节作为扩展名，4字节作为大小。
### 32字节功能如下

| 字节偏移 | 大小（字节） | 含义 |
|----------|---------------|------|
| `0x00`   | 11            | 文件名（8字节）+ 扩展名（3字节），符合8.3格式 |
| `0x0B`   | 1             | 属性（如只读、系统文件、目录等） |
| `0x0C`   | 1             | 保留（通常为0） |
| `0x0D`   | 1             | 创建时间（毫秒） |
| `0x0E`   | 2             | 创建时间（小时+分钟+秒） |
| `0x10`   | 2             | 创建日期 |
| `0x12`   | 2             | 最后访问日期 |
| `0x14`   | 2             | 高 16 位起始 cluster（FAT32独有） |
| `0x16`   | 2             | 最后修改时间 |
| `0x18`   | 2             | 最后修改日期 |
| `0x1A`   | 2             | 低 16 位起始 cluster |
| `0x1C`   | 4             | 文件大小（单位：字节） |


# 实模式到保护模式到长模式
### 定义GDT 全局描述符 (临时使用内存分段，之后会全部切换到内存分页)
- 包含段描述符（*空描述符、*数据段描述符、*代码段描述符、调用门，任务门） *为必须
- LDT
- 调用门
- 任务门等
### 定义GDT（全局描述符表）后告知CPU 
- GDT:
	dw GDTSize (16bit)
	dd GDTBase (32bit内存地址)
	描述一个GDT要48bit
- LGDT:
	LGDT Address ;从指定地址读取6个字节（48bit），得到的数据保存到GDTR

-	修改CPU CR0的PE位,清空流水线:

```nasm
EnableProtectModel:
	mov eax,0x40000023
	mov cr0,eax
	jmp CodeDes: dword ProtectLand
	
	BITS 32 ;告诉编译器后面的代码是32位的
	ProtectLand:
		mov  eax, 0x0000000A
	
```

### 描述符-概括
- size 8字节:64bit 
- 段基地址32bit : 分为两块、分散存储在64bit描述符中
- 段界限20bit:  分为两块、分散存储在64bit描述符中
- 段属性12bit:  分为两块、分散存储在64bit描述符中

 为了保证对过去代码的兼容性，要拆开，0-47为了兼容80286，48-63位为32位系统新增的
#### 段属性
	描述这个段是：
	- 代码段还是数据段，
	- 权限
	- p位:当前指向的段位于内存中还是被交换到硬盘
	- 用作堆还是栈（关系到扩展方向，栈扩展方向是越来越小）
	- G位:1每个地址表示的是4k，0一个字节
	- S位: 0表示这个段属于系统段，权限比较高，1表示数据段或者代码段
	- DPL:描述段权限，如果cpu上CPL小于DPL，才可以取地址，否则权限不足
	- 
完整说明

| 位名       | 位宽 | 位置（描述符位） | 作用说明                                                                 |
|------------|------|------------------|--------------------------------------------------------------------------|
| **P（Present）**     | 1    | 位47             | 段是否存在于内存中。0表示段不存在，会触发 #NP 异常。1表示段存在。             |
| **DPL（Descriptor Privilege Level）** | 2    | 位45-46          | 段的特权级，范围 0（最高）~3（最低）。用于权限检查。                        |
| **S（Segment Type）** | 1    | 位44             | 1 表示代码/数据段；0 表示系统段（如 TSS、LDT、门等）。                      |
| **TYPE（段类型）**   | 4    | 位40-43          | 描述段的具体类型（代码段、数据段、系统段等），含读写、执行、扩展等标志。     |
| **G（Granularity）** | 1    | 位55             | 段界限粒度。0 表示以字节为单位；1 表示以 4KB 为单位。                       |
| **D/B（Default Operation Size / Big）** | 1    | 位54             | 对代码段：0=16位代码，1=32位代码；对栈段：0=使用 SP，1=使用 ESP。           |
| **L（64-bit Code Segment）** | 1    | 位53             | 仅在 IA-32e 模式下使用。1 表示该段为 64 位代码段。                          |
| **AVL（Available for use by system software）** | 1    | 位52             | 操作系统可自由使用的位，处理器不使用。                                     |
| **段界限（Limit）** | 20   | 位0-15 和 48-51   | 段的最大偏移地址。结合 G 位决定单位（字节或 4KB）。                         |
| **段基址（Base）** | 32   | 位16-39 和 56-63  | 段的起始地址。由三部分组成：Base Low、Base Middle、Base High。             |
### 描述符-空描述符
	- 全是0，用于初始化段选择子
### 描述符-代码段描述符
		
### 描述符-数据段描述符

# 内存分页

### 内存被切割为4kb一页大小2^12，所以4G内存一共有2^20个4k页面
- 页表：描述一页要4字节内存(高20位地址+底12位页表属性)，页表占用空间2^20*4字节 =4MiB空间
- 页表目录:1、由于页表本身可能不连续（单依旧按照最小4k连续），需要描述这4MiB内存的入口，
		  2、因此需要，4M/4k = 1024个页表目录项目，所以32位系统中，页表目录有1024个条目，一个条目有4字节
		  3、CR3用于保存页目录内存地址，类似于GDTR
		  4、每个程序单独一份页目录（PDT）、页表（PT）、页结构，CPU上下文切换时CR3切换到其他页表目录
- 32位线性地址/虚拟地址: 一个32位地址按照10-10-12查询（页表编号，内存页编号，偏移地址），分别代表页表编号、内存页编号(一页内存4k)、页内偏移(12bit，刚好表示0-4k的范围)

- 虚拟地址访问: cr3查询页表目录地址，页表目录偏移:第一个10bit表示的数*4,取出内存页表地址（20bit的物理地址，但是没有低12位），
		    内存页表地址偏移:第二个10bit表示的数*4，取出内存页地址（20bit的物理地址，但是没有低12位），
			物理内存页地址+12位偏移地址就是实际物理地址
		  
- 小结：4k分页下，4G内存需要4M页表内存描述，4M页表内存需要4k页表目录描述，
		内存分页，页表和页表目录构成了一个管理内存的三层结构
		cr3是64bit,32bit模式下使用低32bit，31-12存储物理地址，末尾的12位始终是只有3-4 bit有效，
		页表目录1024项，所以假如页表目录查询到的最小连续地址，实际上隔着4M物理内存（4G/1024=4M）。
		内存页编号是20bit（两两内存编号之间子最小连续地址实际差距是4k），所以直接拼接12bit偏移，得到最终物理地址
### 内存碎片
- 程序的段一般为，数据段、代码段、stack ，段必须连续分配，在不停分配与回收过程中，产生非连续空间
- 4k对齐：最后12bit是0 		
 
# x86实际内存权限
### 内存分段
- 系统和程序的权限-系统RPL是0，用户程序 RPL3，占用两个位
### 内存分页
- 由于权限只用0和3，并没有用到中间的，因此到了内存分页系统只用系统的0，用户程序 1，
	内存页属性的U/S位，当U/S=0时页属于超级页，当U/S=1时页属于用户页
- 64位操作系统强制使用内存分页
###  四种分页(arm和risc-v只能用分页，无法分段):
- 32bit paging
- PAE paging
- 4level paging :cr3 64bit全开，64-M是保留位，M-1:12 物理地址（同样是用于线性地址转换）
				4级页表查询流程 PML4 -PDPT-PD-PT,每集页表的元素需要64bit。9-9-9-9-12
- 5 level paging
- hugepage:内存分页为2MiB或者以上，2MiB需要21bit页内寻址，正好需要9-12合并，为9-9-9-21，因此省下一级页表
### 线性地址空间-在一整块内存中，将内存地址连续排列
- 线性地址（Linear Address）是指在处理器内部形成的地址，
  是程序运行时由段基址和偏移量计算得出的地址，也是分页机制之前的地址。
  它是 CPU 用于访问内存的中间地址，与物理地址不同，它必须经过地址转换（如分页）后才能最终访问实际内存单元。
- 对于内存分段，段地址+偏移地址（逻辑地址）组合得到的地址就是线性地址，同时也是物理地址
- 对于内存分页，线性地址是一个虚拟地址，根据页表对应不同的物理设备，可以是io，也可以是物理内存
		
# 四级页表

在四级页表结构中，每一级页表本质上就是一个页大小的表格，用于存储页表项（PTE），而每个页表项通常占用 **8字节（64位系统）**。由于每个页大小为 **4KB（4096字节）**

📐 **每一级页表的空间计算：**
- 每页表大小：**4KB = 4096字节**
- 每个页表项大小：**8字节**
- 每个页表中可容纳的页表项数量：4096/8 = 512

🔢 **结果：**
- 每一级页表占用空间为 **4KB**
- 每一级页表最多包含 **512 个页表项**

🧠 **四级页表结构包括：**
1. **PGD（Page Global Directory）**
2. **PUD（Page Upper Directory）**
3. **PMD（Page Middle Directory）**
4. **PTE（Page Table Entry）**

每一级都占用一个页（4KB），所以在最坏情况下（即所有页表都存在且被填满），
x86-64 四级页表结构中，每一级页表都是一个 4KB 的页，包含 **512 个条目**，每个条目占用 8 字节。这意味着：

- 🧭 **一个 PGD（Page Global Directory）可以包含 512 个 PUD（Page Upper Directory）条目**
- 🧭 **一个 PUD 可以包含 512 个 PMD（Page Middle Directory）条目**
- 🧭 **一个 PMD 可以包含 512 个 PTE（Page Table Entry）条目**
- 每个 PTE 映射一个 4KB 的物理页

📐 **映射能力逐级放大：**
(括号内为x86_64名称。括号外为Linux命名，因为linux要跨平台)

| 层级 | 条目数 | 每条目映射内存 | 总映射能力 |
|------|--------|----------------|-------------|
| PTE  (PT)  | 512    | 4KB            | 2MB         |
| PMD (PD)   | 512    | 2MB            | 1GB         |
| PUD (PDPT) | 512    | 1GB            | 512GB       |
| PGD (PML4) | 512    | 512GB          | 256TB       |

这套设计刚好覆盖了 x86-64 架构下的 **48位虚拟地址空间（2⁴⁸ = 256TB）**

📊 **映射完整 256TB 虚拟地址空间所需的页表空间：约 4MB**

在 x86-64 架构下，采用四级页表结构，每一级页表都是一个页（4KB），每个页表项占用 8 字节，共可容纳 512 个条目。
---

### 🧠 四级页表结构回顾（页大小 4KB）：

| 层级 | 每项映射范围 | 条目数 | 总映射能力 |
|------|----------------|--------|--------------|
| PTE  | 4KB            | 512    | 2MB          |
| PMD  | 2MB            | 512    | 1GB          |
| PUD  | 1GB            | 512    | 512GB        |
| PGD  | 512GB          | 512    | 256TB        |

---

### 🧮 页表空间计算：

- **PGD**：1 页（顶层目录）
- **PUD**：512 页（每个 PGD 条目指向一个 PUD）
- **PMD**：512 × 512 = 262,144 页
- **PTE**：512 × 512 × 512 = 134,217,728 页

每页大小为 4KB：

- PGD：1 × 4KB = 4KB
- PUD：512 × 4KB = 2MB
- PMD：262,144 × 4KB = 1GB
- PTE：134,217,728 × 4KB = **512GB**

理论上的最大值，在每个虚拟地址都被映射时才会达到。
实际系统中采用**按需分配**策略，未使用的地址空间不会分配页表页。

---

### ✅ 使用 Huge Page 的优化：

如果使用 2MiB 的 Huge Page（跳过 PTE 层）：

- 每个 PMD 映射 2MiB
- 映射 256TB 需要：$$\frac{256 \text{TB}}{2 \text{MB}} = 134,217,728$$ 个 PMD 表项
- 所需页表页：
  - PGD：1 页
  - PUD：512 页
  - PMD：134,217,728 ÷ 512 = 262,144 页
- 总空间：1 + 512 + 262,144 = **262,657 页 × 4KB = ≈ 1GB**

如果使用 1GiB Huge Page（跳过 PMD 和 PTE）：

- 仅需 PGD + PUD 页表页：1 + 512 = **513 页 × 4KB = ≈ 2MB**

---

📌 **总结：**

| 页大小 | 最大页表空间 | 是否跳过层级 |
|--------|----------------|----------------|
| 4KB    | ~512GB         | 否             |
| 2MiB   | ~1GB           | 跳过 PTE       |
| 1GiB   | ~2MB           | 跳过 PMD+PTE   |

4k分页下，就算只映射一个页（4k），16kb的固定开销是必须的，16k最大可映射2MiB物理内存 512 × 2^12

- eg:如果使用了4G内存，实际页表开销:
	4096M/2M =2048 * 4 k

	2048 / 512 =4 * 4k

	4 /512 = 1 * 4k

	1/ 512 = 1 * 4k

###  （cache）PAT 与 PWT、PCD 三位组合含义(决定页表缓存在cpu哪个位置，或者是不缓存)
x64 架构中四级分页机制（四级页表）中的每级页表条目（Page Table Entry, PTE）PAT 属性位**的概览表：

### 🧾 PAT 属性位说明表（适用于页表条目）

| 属性位 | 名称               | 说明                                                                                                                                  |
|--------|--------------------|-----------------------------------------------------------------------------------------------------------------------------------------|
| PWT    | Page Write Through | 决定是否启用写穿缓存（Write-Through）。设置为 1 表示使用写穿方式。                                                                   |
| PCD    | Page Cache Disable | 设置为 1 时禁用该页的高速缓存。                                                                                                         |
| PAT    | Page Attribute Table | 这个位在 PD 和 PT 的大页条目中使用，用来与 PAT MSR 配合，选择缓存策略。这个位仅在页大小为 4KB、2MB、1GB 时有所不同的表现。        |

---

- 🧮 PAT 与 PWT、PCD 三位组合含义

根据 Intel 的说明，PAT 实际由 **PAT MSR 的八个条目** 与页表条目的 **PAT/PWT/PCD 三位组合**共同确定页的缓存策略：

| PAT 位 | PWT | PCD | 缓存类型（默认）   |
|--------|-----|-----|---------------------|
| 0      | 0   | 0   | Write-Back（WB）    |
| 0      | 1   | 0   | Write-Through（WT） |
| 0      | 0   | 1   | Uncached（UC−）     |
| 0      | 1   | 1   | Uncached（UC）      |
| 1      | 0   | 0   | Write-Combining（WC）|
| 1      | 1   | 0   | Write-Protected（WP）|
| 1      | 0   | 1   | Uncached（UC）      |
| 1      | 1   | 1   | UC（或实现定义）    |

📌 **注：** PAT 位在 2MB 和 1GB 大页映射时来自 PD/PT 里的第 12 位，而在 4KB 映射时该位来自页表项的第 7 位。

### （cache）全局位 -在 x64 架构的页表条目中，第 8 位（G 位，Global） 
第 8 位 Global 位的作用是什么
- 在页表条目中：

    Global 位（G 位）= 第 8 位（Bit 8）
    设置为 1 时，表示该页是全局页（Global Page）
    设置为 0 时，表示该页是非全局页
-  通常当进程切换或 CR3（页目录基址寄存器）发生变化时，系统会清空 TLB 来避免地址空间混淆。
   设置 Global 位后，这些页表条目会保留在 TLB 中，不被刷新，从而提升访问效率。
 ### x64 架构下页表条目（PTE）字段组成（64位）
 当然可以！下面是一个全面整理的 **x64 架构下页表条目（Page Table Entry, PTE）构成表格**，适用于四级分页中任意一级的页表条目（如 PML4E、PDPE、PDE、PTE）在映射 4KB 页时的基本结构：

---

### 📦 x64 架构下页表条目（PTE）字段组成（64位）

| 位范围      | 名称                  | 说明                                                                 |
|-------------|-----------------------|----------------------------------------------------------------------|
| 0           | Present (P)           | 页是否存在于物理内存中；0 表示页缺页，触发缺页异常                   |
| 1           | Read/Write (R/W)      | 是否允许写入；0 表示只读，1 表示可读写                              |
| 2           | User/Supervisor (U/S) | 是否允许用户态访问；0 表示仅内核态，1 表示用户态可访问              |
| 3           | Page-level Write-through (PWT) | 写穿缓存设置；与缓存策略有关                         |
| 4           | Page-level Cache Disable (PCD) | 禁用缓存；影响内存读写性能与一致性                 |
| 5           | Accessed (A)          | 页是否被访问过；由 CPU 自动设置                                    |
| 6           | Dirty (D)             | 页面是否被写入过（仅对 PTE 有效）；由 CPU 自动设置                   |
| 7           | PAT                   | 缓存属性位，与 PAT MSR 联合决定缓存策略                             |
| 8           | Global (G)            | 是否为全局页；设置后不会因上下文切换而被刷新出 TLB                   |
| 9–11        | 可用位（Ignored）      | 保留给操作系统使用或扩展                                            |
| 12–51       | 页框地址              | 指向物理页帧的地址（物理页帧高位地址，右移 12 位得到页帧地址）      |
| 52–58       | 可用位/保护位         | 可由 OS 定义（如 NX bit 在此范围）                                  |
| 63          | NX (No-Execute)       | 如果 EFER.NXE=1，表示该页不可执行；用于防止代码执行攻击              |

---

- PTE 0-7为属性，常用组合 0-8设置为1
- PTE 9-11 被忽略，常常配置初始值为0
- 12到(M-1) 内存页物理地址，M一般根据物理内存实际大小配置，比如8g内存需要33bit，因为内存分页4k扣掉12bit，需要21bit，12-32为21bit
- M-63 全部设置为0
eg: 物理分页第三页pte（最后一级页表）为0x00000000000031ff

### 🔒 高级选项说明

- **NX 位：** 需要在 MSR `EFER` 寄存器中启用 `NXE`（No eXecute Enable），才可启用。
- **页框地址：** 页表条目仅存储物理页帧的高位地址（低 12 位对齐为 0），因此该字段占位从位 12 到 51。
- **PTE 与 PDE/PDPTE/PML4E 的差异：**  
  - 在 PDE 中设置第 7 位表示 PAT 位用于 2MB 页；
  - 在 PDPTE 中设置第 7 位表示 PAT 位用于 1GB 页；
  - 在 PTE 中第 7 位直接作为 PAT 位用于 4KB 页。

---

# 定义页表(4G物理内存)：
在四级页表结构中，如果希望将 **页大小设为 2MB**，并且物理内存大小为 **4GB**

## 📏 页大小为 2MB 的设计思路

| 层级 | 数量 | 说明 |
|------|------|------|
| PML4 | 1 个 | 指向一个 PDPT 表 |
| PDPT | 1 个 | 指向 4 个 PD 表（每个映射 1GB） |
| PD   | 4 个 | 每个 PD 有 512 个条目，每个条目映射 2MB |
| PT   | 不需要 | 因为使用的是 2MB 大页，跳过这一层 |

---

### 定义第一个PML4E（PGD） :代码为 DQ PDP (0x1000) 
- 在很多 UEFI 固件中，页表是从低地址开始分配的：

    0x0000 ~ 0x0FFF：通常保留或用于中断向量表

    0x1000：是第一个页表（比如 PDP）的起始地址

    0x2000：可能是 PD 表

    0x3000：可能是 PT 表

    依此类推...

  所以 0x1000 并不是PML4的“末尾”，而是PML4第一个页表的地址。
  - 定义完第一级的入口后，通常有剩余部分（除非内存等于256T，否则用不完）
  用0 填充 TIMES 0X1000 - PGTBLS_OFFSET($) DB 0
  
### 定义PDPT(PUD) 
- 位于PML4后面，这两个4k是连在一起的
- dq PDP(0X2000) ;第一个 PDPT 条目，指向第一个 PD 表（地址 0x2000）
  dq PDP(0X3000)
  dq PDP(0X4000)
  dq PDP(0X5000)
  
 TIMES 0X2000 - 当前地址 db 0  ;8k的位置-当前地址，填充0
 
 
### 难点分析:
在 EDK II 中，**PML4 表并不是位于地址 `0x1000`**。实际上，
`DQ PDP (0x1000)` 是在定义 **PML4 表的第一个条目**，它指向 PDP 表的地址 `0x1000`，
而不是表示 PML4 表本身位于该地址。

---

## 📍 PML4 的实际位置在哪里？

根据 EDK II 的页表初始化逻辑，尤其是在 `ResetVector` 阶段，页表的布局通常是：

### 🧭 页表布局（以 2MB 页为例）

| 地址       | 内容              |
|------------|-------------------|
| `0x1000`   | PDP 表（PDPT）     |
| `0x2000`   | PD 表（映射 0–1GB）|
| `0x3000`   | PD 表（映射 1–2GB）|
| `0x4000`   | PD 表（映射 2–3GB）|
| `0x5000`   | PD 表（映射 3–4GB）|
| `0x6000`   | 其他页表或保留     |
| `0x7000`   | 其他页表或保留     |
| `0x8000`   | 其他页表或保留     |
| `0x9000`   | 其他页表或保留     |
| `0xA000`   | 其他页表或保留     |
| `0xB000`   | 其他页表或保留     |
| `0xC000`   | 其他页表或保留     |
| `0xD000`   | 其他页表或保留     |
| `0xE000`   | 其他页表或保留     |
| `0xF000`   | **PML4 表**        |

所以，**PML4 表通常放在页表区域的最高地址，例如 `0xF000` 或 `4GB - 4KB`**，
以便在设置 `CR3` 寄存器时使用一个固定的、易于管理的地址。

---

- 🧠 为什么这样设计？

- 页表是手动构建的，开发者可以自由选择地址
- 将 PML4 放在高地址可以避免与其他内存区域冲突
- 在 `CR3` 寄存器中设置页表基地址时，使用高地址更安全、更清晰

---

- 🔍 代码中的 `DQ PDP (0x1000)` 是什么意思？

这行代码的含义是：

- 在当前地址（比如 `0xF000`）写入一个 8 字节的值
- 这个值是 PDP 表的物理地址 `0x1000`
- 也就是：**PML4[0] = 0x1000 | flags**

这就是构建 PML4 表的第一个条目。

---

- 总结

- `DQ PDP (0x1000)` 是在构建 PML4 表的条目，不是定义 PML4 表的位置
- PML4 表通常放在高地址，比如 `0xF000` 或 `4GB - 4KB`
- PDP 表才是位于 `0x1000`，是 PML4[0] 所指向的目标
- 页表地址布局是人为设计的，EDK II 中采用的是“高地址放 PML4”的策略

---